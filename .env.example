# Contact API configuration
# Copy to .env/.env.local and provide the endpoint that accepts POST contact submissions.
VITE_CONTACT_ENDPOINT=https://api.example.com/contact

# Optional: override request timeout (milliseconds)
VITE_CONTACT_TIMEOUT=10000

# Optional: monitoring + deployment metadata
VITE_SENTRY_DSN=
VITE_APP_VERSION=
VITE_APP_ENV=development
